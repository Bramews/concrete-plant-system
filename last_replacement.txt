        <div className="flex justify-between items-center p-2 border-b border-slate-800 shrink-0">
          <div className="flex flex-col gap-0.5">
            <div className="flex items-center gap-2">
              <h3 className="text-xs font-bold text-white flex items-center gap-1">
                <span className="w-5 h-5 rounded-lg bg-indigo-500/20 text-indigo-400 flex items-center justify-center">
                  <svg className="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z"></path></svg>
                </span>
                الضبط الهندسي للخلطة
              </h3>
              <span className="text-[10px] text-indigo-400 font-bold bg-indigo-500/10 px-1.5 py-0.5 rounded-md">طلب رقم: {order.orderNumber}</span>
            </div>
            
            <div className="flex flex-wrap items-center gap-x-2.5 gap-y-0.5 text-[10px] text-slate-400 mt-0.5">
              <span className="bg-slate-800/40 px-1.5 py-0.5 rounded border border-slate-700/30">
                <span className="text-slate-400 font-bold">المشروع: </span>
                <span className="text-white font-medium">
                  {(() => {
                    const loc = order.project?.location || "";
                    const idx = loc.search(/\((GPS|FILE):|GPS:|FILE:/);
                    const clean = idx !== -1 ? loc.substring(0, idx).trim() : loc.trim();
                    return clean || order.project?.name || "غير محدد";
                  })()}
                </span>
              </span>
              <span className="bg-slate-800/40 px-1.5 py-0.5 rounded border border-slate-700/30">
                <span className="text-slate-400 font-bold">الكمية: </span>
                <span className="text-indigo-300 font-mono font-bold">{order.originalQuantity} m³</span>
              </span>
              <span className="bg-slate-800/40 px-1.5 py-0.5 rounded border border-slate-700/30">
                <span className="text-slate-400 font-bold">كود الخلطة: </span>
                <span className="text-white font-mono font-bold">{order.mixDesign?.code || "N/A"}</span>
              </span>
              <span className="bg-slate-800/40 px-1.5 py-0.5 rounded border border-slate-700/30">
                <span className="text-slate-400 font-bold">المقاومة: </span>
                <span className="text-white font-mono font-bold">{order.mixDesign?.strength || "N/A"} MPa</span>
              </span>
            </div>
          </div>
          <button onClick={() => setIsOpen(false)} className="text-slate-400 hover:text-white bg-slate-800/50 hover:bg-slate-800 p-1 rounded-lg transition-colors">
            <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M6 18L18 6M6 6l12 12"></path></svg>
          </button>
        </div>

        <div className="flex-1 overflow-y-auto p-2 py-1.5 space-y-1.5 custom-scrollbar">
          {!isRejectMode ? (
            <>
              {/* 1. Moisture Settings & Admixture Controls - Full Width */}
              <div className="bg-slate-800/30 border border-slate-700/50 rounded-xl p-2 py-1.5 shadow-sm">
                <h4 className="text-indigo-400 font-bold mb-1 text-[11px] flex items-center gap-1.5">
                  <svg className="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M3 15a4 4 0 004 4h9a5 5 0 10-.1-9.999 5.002 5.002 0 10-9.78 2.096A4.001 4.001 0 003 15z"></path></svg>
                  إعدادات الرطوبة وتعديل المضاف
                </h4>
                <div className="grid grid-cols-4 gap-2">
                  <div className="space-y-0.5 text-center">
                    <label className="text-[10px] text-slate-300 font-bold block">رطوبة الرمل (%)</label>
                    <input 
                      type="number" 
                      min="0" max="20" step="0.1" 
                      value={sandMoisture} 
                      onChange={(e) => setSandMoisture(e.target.value)}
                      className="w-full bg-slate-950/60 border border-slate-700 rounded-lg py-0.5 px-2 text-center text-xs text-white focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 transition-all font-mono font-bold"
                      placeholder="."
                    />
                  </div>
                  
                  <div className="space-y-0.5 text-center">
                    <label className="text-[10px] text-slate-300 font-bold block">رطوبة الحصى (%)</label>
                    <input 
                      type="number" 
                      min="0" max="20" step="0.1" 
                      value={gravelMoisture} 
                      onChange={(e) => setGravelMoisture(e.target.value)}
                      className="w-full bg-slate-950/60 border border-slate-700 rounded-lg py-0.5 px-2 text-center text-xs text-white focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 transition-all font-mono font-bold"
                      placeholder="."
                    />
                  </div>

                  <div className="space-y-0.5 text-center">
                    <label className="text-[10px] text-slate-300 font-bold block">رطوبة البحص (%)</label>
                    <input 
                      type="number" 
                      min="0" max="20" step="0.1" 
                      value={bahasMoisture} 
                      onChange={(e) => setBahasMoisture(e.target.value)}
                      className="w-full bg-slate-950/60 border border-slate-700 rounded-lg py-0.5 px-2 text-center text-xs text-white focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 transition-all font-mono font-bold"
                      placeholder="."
                    />
                  </div>

                  <div className="space-y-0.5 text-center">
                    <label className="text-[10px] text-slate-300 font-bold block">تعديل المضاف (%)</label>
                    <input 
                      type="number" 
                      step="0.05" 
                      value={admixturePctAdjustment} 
                      onChange={(e) => setAdmixturePctAdjustment(e.target.value)}
                      className="w-full bg-slate-950/60 border border-slate-700 rounded-lg py-0.5 px-2 text-center text-xs text-white focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 transition-all font-mono font-bold"
                      placeholder="."
                    />
                  </div>
                </div>
              </div>

              {/* 2. Logistics & Recommendations - Side by Side (ETA on right, Recommendations on left) */}
              <div className="grid grid-cols-1 md:grid-cols-3 gap-2">
                {/* Logistics / ETA (Takes 1 column, appears on the right in RTL) */}
                <div className="col-span-1 bg-slate-800/30 border border-slate-700/50 rounded-xl p-1.5 py-1 shadow-sm flex flex-col justify-center">
                  <h4 className="text-slate-300 font-bold mb-0.5 text-[10px] flex items-center gap-1.5">
                    وقت الوصول (ETA)
                  </h4>
                  <input 
                    type="time" 
                    value={eta} 
                    onChange={(e) => setEta(e.target.value)}
                    className="w-full bg-slate-900 border border-slate-700 rounded-lg py-0.5 px-2 text-xs text-white focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 transition-all font-mono"
                  />
                </div>

                {/* Recommendations (Takes 2 columns, appears on the left in RTL) */}
                <div className="col-span-2 space-y-0.5">
                  <label className="text-[10px] font-bold text-slate-300 flex items-center gap-1.5">
                    توصيات لمشغل المحطة
                  </label>
                  <textarea 
                    value={details}
                    onChange={(e) => setDetails(e.target.value)}
                    className="w-full bg-slate-900 border border-slate-700 rounded-lg p-1 text-[11px] text-white placeholder-slate-500 focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 transition-all min-h-[30px] shadow-inner"
                    placeholder="توصيات اختيارية للمشغل..."
                  />
                </div>
              </div>

              {/* 3. Table - Full Width */}
              <div className="bg-slate-900/50 rounded-xl border border-slate-700 overflow-hidden shadow-inner w-full">
                <table className="w-full text-[9px] text-right">
                  <thead className="bg-slate-800 border-b border-slate-700">
                    <tr>
                      <th className="py-0.5 px-1 font-bold text-slate-300 w-1/3">المادة</th>
                      <th className="py-0.5 px-1 font-bold text-slate-300">الوزن التصميمي</th>
                      <th className="py-0.5 px-1 font-bold text-indigo-300">الوزن المعتمد (بعد التعديل)</th>
                      <th className="py-0.5 px-1 font-bold text-slate-300">التغيير</th>
                    </tr>
                  </thead>
                  <tbody className="divide-y divide-slate-800">
                    {computedMaterials.length === 0 ? (
                      <tr>
                        <td colSpan={4} className="p-1 text-center text-slate-500">
                          لا توجد مواد مسجلة في تفاصيل الخلطة.
                        </td>
                      </tr>
                    ) : (
                      computedMaterials.map((c: any) => (
                        <tr key={c.id} className="hover:bg-slate-800/30 transition-colors">
                          <td className="py-0.5 px-1 flex items-center gap-1">
                            {c.isWater ? <span className="w-1.5 h-1.5 rounded-full bg-cyan-400"></span> :
                             c.isSand ? <span className="w-1.5 h-1.5 rounded-full bg-amber-400"></span> :
                             c.isGravel ? <span className="w-1.5 h-1.5 rounded-full bg-slate-400"></span> :
                             c.isAdmix ? <span className="w-1.5 h-1.5 rounded-full bg-purple-400"></span> :
                             <span className="w-1.5 h-1.5 rounded-full bg-emerald-400"></span>}
                            <span className="font-medium text-slate-200">{c.materialNameArabic}</span>
                          </td>
                          <td className="py-0.5 px-1 text-slate-400 font-mono">
                            {c.isAdmix ? (
                              <div className="flex items-center gap-0.5">
                                <span>{c.baseQty.toFixed(2)} %</span>
                                <span className="text-[7px] text-slate-500 bg-slate-800 px-0.5 rounded">({c.admixLiters.toFixed(2)} L)</span>
                              </div>
                            ) : (
                              <>{c.baseQty.toFixed(1)} {c.unit}</>
                            )}
                          </td>
                          <td className={`py-0.5 px-1 font-bold font-mono ${c.isWater ? 'text-cyan-400' : c.isSand ? 'text-amber-400' : c.isGravel ? 'text-slate-200' : c.isAdmix ? 'text-purple-400' : 'text-emerald-400'}`}>
                            {c.isAdmix ? (
                              <div className="flex items-center gap-0.5">
                                <span>{c.adjustedQuantity.toFixed(2)} %</span>
                                <span className="text-[7px] opacity-60 bg-slate-800 px-0.5 rounded">({c.admixAdjustedLiters.toFixed(2)} L)</span>
                              </div>
                            ) : (
                              <>{c.adjustedQuantity.toFixed(1)} {c.unit}</>
                            )}
                          </td>
                          <td className="py-0.5 px-1 font-mono" dir="ltr">
                            {c.isAdmix ? (
                              c.change === 0 ? (
                                <span className="text-slate-600">-</span>
                              ) : c.change > 0 ? (
                                <span className="text-purple-400 bg-purple-400/10 px-1 py-0.5 rounded text-[7px] inline-flex items-center gap-0.5">
                                  +{c.change.toFixed(2)}%
                                  <span className="opacity-70 text-[6px]">(+{c.admixChangeLiters.toFixed(2)}L)</span>
                                </span>
                              ) : (
                                <span className="text-rose-400 bg-rose-400/10 px-1 py-0.5 rounded text-[7px] inline-flex items-center gap-0.5">
                                  {c.change.toFixed(2)}%
                                  <span className="opacity-70 text-[6px]">({c.admixChangeLiters.toFixed(2)}L)</span>
                                </span>
                              )
                            ) : (
                              c.change === 0 ? (
                                <span className="text-slate-600">-</span>
                              ) : c.change > 0 ? (
                                <span className="text-emerald-400 bg-emerald-400/10 px-1 py-0.5 rounded">+{(c.change).toFixed(1)}</span>
                              ) : (
                                <span className="text-rose-400 bg-rose-400/10 px-1 py-0.5 rounded">{(c.change).toFixed(1)}</span>
                              )
                            )}
                          </td>
                        </tr>
                      ))
                    )}
                  </tbody>
                </table>
              </div>
            </>
          ) : (
            <div className="bg-rose-950/30 border border-rose-500/30 rounded-xl p-6 text-center animate-in zoom-in-95 duration-200">
              <div className="w-16 h-16 bg-rose-500/20 text-rose-500 rounded-full flex items-center justify-center mx-auto mb-4 shadow-inner">
                <svg className="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path></svg>
              </div>
              <h4 className="text-xl font-bold text-rose-400 mb-2">تأكيد رفض الطلبية</h4>
              <p className="text-slate-300 text-sm max-w-md mx-auto">أنت على وشك رفض هذه الطلبية بشكل نهائي وإعادتها لقسم المبيعات للتعديل أو الإلغاء. يرجى توضيح السبب بدقة في حقل الملاحظات.</p>
            </div>
          )}
        </div>

        {/* Action Buttons */}
        <div className="p-1.5 px-3 border-t border-slate-800 flex flex-wrap gap-2 justify-end shrink-0 bg-slate-900/50 rounded-b-xl">
          {!isRejectMode ? (
            <>
              <button
                disabled={isSubmitting}
                onClick={() => setIsOpen(false)}
                className="btn bg-slate-800 py-1 px-3 text-[11px] text-slate-300 hover:bg-slate-700 hover:text-white transition-all flex-1 sm:flex-none font-bold"
              >
                إلغاء الأمر
              </button>
              <button
                disabled={isSubmitting}
                onClick={() => setIsRejectMode(true)}
                className="btn bg-rose-500/10 py-1 px-3 text-[11px] text-rose-500 border border-rose-500/30 hover:bg-rose-600 hover:text-white hover:border-rose-600 transition-all flex-1 sm:flex-none font-bold"
              >
                رفض وإرجاع
              </button>
              <button
                disabled={isSubmitting}
                onClick={handleApprove}
                className="btn btn-primary py-1 px-4 text-[11px] flex-1 sm:flex-none min-w-[160px] font-bold shadow-lg shadow-indigo-500/20"
              >
                {isSubmitting ? (
                  <span className="flex items-center gap-2 justify-center">
                    <svg className="animate-spin -ml-1 mr-2 h-3.5 w-3.5 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"><circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4"></circle><path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path></svg>
                    جاري الاعتماد...
                  </span>
                ) : "اعتماد الأوزان وإرسال"}
              </button>
            </>
          ) : (
            <>
              <button
                disabled={isSubmitting}
                onClick={() => setIsRejectMode(false)}
                className="btn bg-slate-800 py-1 px-3 text-[11px] text-slate-300 hover:bg-slate-700 hover:text-white transition-all flex-1 sm:flex-none font-bold"
              >
                تراجع
              </button>
              <button
                disabled={isSubmitting}
                onClick={handleReject}
                className="btn bg-rose-600 py-1 px-4 text-[11px] text-white hover:bg-rose-700 transition-all flex-1 sm:flex-none min-w-[160px] font-bold shadow-lg shadow-rose-600/20"
              >
                {isSubmitting ? "جاري الرفض..." : "تأكيد الرفض النهائي"}
              </button>
            </>