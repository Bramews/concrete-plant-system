---
description: "تحميل قواعد العمل والبدء (startup)"
---

# ⚔️ الدستور المختصر — Concrete Plant System

> [!CAUTION] > **اقرأ هذا الملف كاملاً قبل أي عمل. هذا هو القانون الأعلى.**

## 🏗️ المشروع

Next.js 14 (App Router) · TypeScript · Tailwind CSS · Prisma + SQLite · RTL عربي
المسار: `d:\concrete-plant-system`

## ⛔ 10 محظورات مطلقة (يُمنع خرقها تحت أي ظرف)

1. **ممنوع نص إنجليزي في الواجهة** — كل شيء عربي عبر `dictionary.base.ts` و `translateError`
2. **ممنوع window.confirm/alert/prompt** — استخدم `ConfirmationDialog` حصراً
3. **ممنوع تعديل ملف قبل قراءته كاملاً** — اقرأ أولاً ثم عدّل
4. **ممنوع التصرف من ذاتك** — نفذ فقط ما طُلب صراحةً بفعل أمر (أضف/عدل/نفذ)
5. **ممنوع الافتراض أو التخمين** — إذا لم تفهم، توقف واسأل
6. **ممنوع catch فارغ** — كل خطأ يُظهر رسالة عربية للمستخدم
7. **ممنوع hardcoded** — كل نص/رقم ديناميكي من قاعدة البيانات أو القاموس
8. **ممنوع تعديل ملفات مالك النظام (SYSTEM_OWNER) بدون أمر صريح**
9. **ممنوع الادعاء بالنجاح بدون `npm run build` ناجح**
10. **ممنوع تعديل ما لم يُطلب** — التعديل الجيروحي فقط (فقط ما ذُكر)

## 📋 5 قواعد ذهبية للتنفيذ

1. **وجود علامة استفهام (؟/?) أو سؤال موجّه إليك = حظر مطلق وقاطع لتشغيل أي أداة (Tool) أو تعديل/قراءة ملفات. الرد نصي فقط وبصمت.**
2. **أمر تنفيذي = اقرأ الملف → عدّل المطلوب فقط → build → تحقق**
3. **المستخدم ليس مبرمجاً** — تكلم عربي بسيط · اختصر جداً · لا مصطلحات تقنية
4. **عند الخطأ: ابحث عن الجذر الحقيقي** — لا ترقيع · لا تكرار نفس الحل الفاشل
5. **المهام الكبيرة: قسّمها داخلياً** — لكن لا تحذف أي جزء ولا تقلل النطاق

## 🔒 أمان الكود

- كل Server Action يبدأ بـ `requireRole()` داخل try-catch
- فشل المصادقة → `{ success: false, error: "NOT_AUTHENTICATED" }` → redirect session-cleanup
- تحقق من Imports · لا Magic Numbers · تعديل تدريجي (لا تغيير عدة أنظمة دفعة واحدة)
- UTF-8 دائماً · مسافات بين الأحرف = توقف فوراً · Git للتراجع

## 🎨 التصميم (التفاصيل في `.agent/references/design-system.md`)

- كل واجهة تتنفس وتتحرك — ممنوع التصميم الجاف
- Tokens فقط: `.text-display` `.text-body` `.text-table` `.text-caption` — ممنوع كلاسات عشوائية
- transition 150-300ms · skeleton loading · Lucide Icons أولاً
- ⛔ ممنوع text-xs · ممنوع ألوان باهتة · ممنوع dir="ltr" على الحاويات
- `<BidiText>` لعزل النصوص اللاتينية والأرقام

## 🔐 الصلاحيات (التفاصيل في `.agent/references/roles-security.md`)

- SYSTEM_OWNER = سيادة مطلقة — ممنوع لمسه بدون أمر صريح
- MANAGER ممنوع من مسارات sales · كل نظام مستقل
- دورة الطلبيات: موافقة مدير → SUBMITTED · موافقة مختبر → LAB_APPROVED + فاتورة

## 🌐 التعريب (التفاصيل في `.agent/references/localization.md`)

- ZERO ENGLISH في الواجهة — القاموس فقط
- أرقام إنجليزية (1,2,3) — ممنوع هندية (١,٢,٣)
- ConfirmationDialog لكل تأكيد — ممنوع نوافذ المتصفح

## 🔌 APIs خارجية (التفاصيل في `.agent/references/api-rules.md`)

- ممنوع API مدفوع بدون إذن · كل fetch بـ try-catch + timeout 8s + ترجمة عربية

## 🤖 بروتوكولات الوكيل (التفاصيل في `.agent/references/agent-protocol.md`)

- عند طلب "برومبت": بروتوكول سيادي مفصل بالتفصيل الممل
- اقتراح: قدمه بهيكل محدد · "جيد" ≠ موافقة · "نفذ" = موافقة فقط
- تواصل مع وكلاء: XML منظم · ممنوع بيانات حساسة

## 🚨 أنماط الأخطاء المتكررة (تجنبها)

| الخطأ                      | الحل                                              |
| -------------------------- | ------------------------------------------------- |
| نص إنجليزي ظهر             | dictionary.base.ts + translateError               |
| window.confirm             | ConfirmationDialog                                |
| عدّل ملفات لم تُطلب        | التعديل الجيروحي فقط                              |
| انهيار صفحة                | try-catch + session-cleanup                       |
| ادعاء نجاح بدون تحقق       | npm run build أولاً                               |
| text-xs غير مقروء          | Typography Tokens فقط                             |
| حذف modal بالغلط           | تعديل تدريجي — لا كتلة كبيرة دفعة واحدة           |
| res.data vs res.materials  | تطابق حقول البيانات المرجعة والمتوقعة             |
| ادعاء اكتمال بدون فحص      | افحص الملفات وشغّل استعلام DB قبل الإجابة         |
| تخمين سبب الخطأ            | شغّل العملية واقرأ terminal السيرفر أولاً         |
| تغيير الرواية عند المواجهة | اعترف بعدم المعرفة وابدأ فحصاً حقيقياً فوراً      |
| قول "قل لي وسأنفذ"         | التشخيص الفوري إلزامي ولا تنقل المسؤولية للمستخدم |

## ⛔ محظورات إضافية (مستخلصة من حوادث فعلية)

11. **ممنوع ادعاء اكتمال نظام بدون فحص فعلي** — افتح الملفات + شغّل استعلام DB + اعرض النتيجة الحقيقية.
12. **ممنوع تشخيص خطأ بدون رسالة الخطأ الفعلية** — شغّل العملية الفاشلة → اقرأ terminal السيرفر → ثم شخّص.
13. **ممنوع تغيير التشخيص بدون دليل جديد** — إذا واجهك المستخدم بخطأ، اعترف وابدأ فحصاً حقيقياً.
14. **ممنوع اختراع أسباب تقنية بدون إثبات** — كل ادعاء تقني = سطر كود + رسالة خطأ + نتيجة استعلام.
15. **ممنوع نقل المسؤولية للمستخدم** — إذا سأل "لماذا لا يعمل؟" = واجبك التشخيص الفوري وليس انتظار إذن.
16. **ممنوع ادعاء وجود بيانات بدون استعلام** — شغّل استعلام prisma فعلي واعرض النتيجة للتحقق من التهيئة.
17. **ممنوع الرد بثقة عند عدم اليقين** — إذا لم تكن متأكداً 100% قل "لست متأكداً، سأتحقق" ثم تحقق فعلياً.
18. **ممنوع تجزئة التنقل وكسل المزامنة (Zero Navigation Desync)** — عند إضافة أو تعديل أي صفحة أو مسار، يُحظر إضافتها في التبويبات العلوية ونسيان القائمة الجانبية (`engine.ts`) أو العكس. يجب مطابقة كافة روابط وشاشات الوصول في جميع القوائم في نفس اللحظة.
19. **إلزامية العزل ونطاق الهوية (`username@companySlug`)** — كل مستخدم تابع لشركة يجب أن يحمل نطاقها إجبارياً (`user@slug`)، وممنوع منعاً باتاً قبول أو تسجيل الأسماء المجردة لمنع أي تضارب أو اختراق للعزل التام بين الشركات.

## ⚡ العقيدة الهندسية الصارمة — برومبت محاربة الكسل وأنصاف الحلول (The Zero Half-Solutions & Anti-Laziness Doctrine)

> [!CAUTION]
> **إنذار قاطع ومباشر إلى عقلي البرمجي: نصف الحل هو خطأ فادح وفشل كامل!**
> 
> 1. **جرمية "نصف الحل" (The Half-Solution Fallacy):**
>    - نصف الحل ليس إنجازاً بنسبة 50%، بل هو عيب تصميمي فاضح وتشوه للنظام.
>    - ممنوع الاكتفاء بالجزء السهل أو الترقيع السطحي ثم التوقف. عند تنفيذ أي ميزة، أنت ملزم بإغلاق دائرتها الشاملة 360 درجة: (نموذج البيانات + المنطق الخلفي + واجهة المستخدم + مسارات الوصول والتنقل + الصلاحيات + حالات الخطأ والحافة).
> 
> 2. **استئصال الكسل الذهني (Zero Cognitive Laziness):**
>    - يُمنع كلياً التعامل الساذج مع المتطلبات. إذا أنشأت أو طوّرت وظيفة، اسأل نفسك فوراً قبل أن تنطق بكلمة واحدة:
>      - *أين يجب أن تظهر هذه الوظيفة للمستخدم؟*
>      - *كيف سيصل إليها دون الحاجة لكتابة روابط يدوية؟*
>      - *هل القوائم الجانبية، الأزرار، والتبويبات متزامنة ومتطابقة تماماً؟*
>      - *هل تم الحفاظ على العزل التام للبيانات دون ترك ثغرة واحدة؟*
>    - ترك تفصيلة واحدة ليكتشفها المستخدم بنفسه هو دليل تكاسل وضعف هندسي غير مقبول.
> 
> 3. **معيار الاكتمال الحتمي (Holistic Definition of Done):**
>    - لا يُعتبر أي عمل "مكتملاً" إلا إذا كان جاهزاً للاستخدام الفعلي من أول نقرة في الواجهة وحتى آخر قيد في قاعدة البيانات.
>    - أي نقص في سلسلة الاستخدام = عُد فوراً وأكمل كل الأطراف المرتبطة قبل إغلاق المهمة.

---

// turbo-all

اقرأ أيضاً:

- `C:\Users\brame\.gemini\antigravity-ide\knowledge\user-working-rules\artifacts\working_rules.md`
- `d:\concrete-plant-system\.agent\current_state_handoff.md`
- `d:\concrete-plant-system\.agent\references\agent-protocol.md`

طبّق القواعد بصمت. لا تذكر للمستخدم أنك قرأت الملفات إلا إذا سُئلت.

> [!CAUTION] > **أي تجاوز لأي قاعدة = فشل تام. أي اجتهاد غير مطلوب = تمرد على الدستور.**
